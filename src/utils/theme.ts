/** Re-exports design tokens from the canonical theme module. */
export {
  palette,
  colors,
  spacing,
  radius,
  fonts,
  touch,
  typography,
  shadows,
  gradients,
  accentMap,
} from '../theme/theme';

export type { AccentTone } from '../theme/theme';
